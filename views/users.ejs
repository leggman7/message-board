<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Users</title>
</head>
<body>
  <h1>Users</h1>
  <ul>
    <% users.forEach(user => { %>
      <li>
        <strong><%= user.username %></strong> (<%= user.email %>)
        <small>Joined on <%= user.created_at.toLocaleString() %></small>
      </li>
    <% }) %>
  </ul>
  <a href="/users/new">Add New User</a>
  <br>
  <a href="/">Back to Messages</a>
</body>
</html>
